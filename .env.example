# Copy to .env.development or .env.production and fill in values.
# Never commit real secrets.

NODE_ENV=development

# TCP server
TCP_HOST=0.0.0.0
TCP_PORT=5027

# MongoDB Atlas (same DB as IoT Dashboard)
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER/DATABASE?retryWrites=true&w=majority

# Logging: fatal | error | warn | info | debug
LOG_LEVEL=debug

# strict = IMEI must exist in Devices collection
# open   = accept any valid IMEI format (lab only)
IMEI_AUTH_MODE=strict

# Idle socket timeout (ms)
SOCKET_IDLE_TIMEOUT_MS=300000

# Max bytes buffered per connection (memory protection)
MAX_BUFFER_BYTES=1048576

# Modbus / Codec 12 (internal readRegister service)
MODBUS_RESPONSE_TIMEOUT_MS=8000
MODBUS_MAX_RETRIES=1
MODBUS_QUEUE_MAX_PER_IMEI=50
# true = log Codec12/Modbus hex + timing (development only)
MODBUS_DEBUG=false

# Optional live Frequency read — runs once AFTER this IMEI authenticates (not at startup)
# MODBUS_LIVE_READ_IMEI=352094081234567
# Delay after auth before sending Codec12 (default 4000 ms)
# MODBUS_LIVE_READ_DELAY_MS=4000
